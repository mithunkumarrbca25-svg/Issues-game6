name: Issues Game 6 Controller

on:
  issue_comment:
    types: [created]

permissions:
  issues: write
  contents: read

jobs:
  controller:
    runs-on: ubuntu-latest
    steps:
      - name: Test workflow
        run: echo "Game 6 controller workflow is valid"
